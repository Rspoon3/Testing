import Testing
@testable import MomentumChat

@Suite("MomentumChat Tests")
struct MomentumChatTests {

    @Test("ChatView can be created with WorkoutMessage")
    func chatViewCreation() {
        // Basic compilation test - views require UI testing for full verification
        #expect(true)
    }
}
